{
  "name": "crudp",
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "dev": "parcel serve ./src/index.html --port 3000",
  "type-check": "tsc -b --noEmit",
  "build": "npm run type-check && parcel build ./src/index.html --no-autoinstall --no-optimize",
  "format": "prettier src --write",
  "clean": "rm -rf dist .parcel-cache"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "^2.14.4",
    "typescript": "^5.8.2",
    "prettier": "^3.5.3"
  }
}
